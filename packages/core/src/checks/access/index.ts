/**
 * Access rule verification. The sharpest finding the tool produces, and the first
 * demonstrable version of the product. No model is involved at any point.
 *
 * Present: rule to plan expansion.
 * Pending: condition evaluation, the deny verdict table, allow verification, list
 * handling, mutating rules, and finding text.
 */
export * from './plan.ts';
