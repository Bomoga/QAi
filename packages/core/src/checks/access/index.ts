/**
 * Access rule verification. The sharpest finding the tool produces, and the first
 * demonstrable version of the product. No model is involved at any point.
 *
 * Present: rule to plan expansion, condition evaluation against a record, and the
 * deny verdict table.
 * Pending: allow verification, list handling, mutating rules, and finding text.
 */
export * from './evaluate.ts';
export * from './plan.ts';
export * from './verdict.ts';
