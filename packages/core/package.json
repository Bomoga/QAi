{
  "name": "@qai/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Schemas, loading, probing, checking, diffing, run assembly, emitters, storage.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "generate:schema": "node --experimental-strip-types scripts/generate-schema.ts",
    "validate:fixture": "node --experimental-strip-types scripts/validate-fixture-spec.ts",
    "capture:goldens": "node --experimental-strip-types scripts/capture-golden-runs.ts"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "fast-glob": "^3.3.3",
    "picocolors": "^1.1.1",
    "undici": "^8.10.0",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0"
  }
}
