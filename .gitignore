node_modules/
dist/
coverage/
*.tsbuildinfo

# Run state: SQLite database, evidence, cached runs
.qai/

.DS_Store
*.log

# Raw corpus run output: one timestamped directory per run, regenerated by
# `node --experimental-strip-types corpus/run.ts`. The durable artifacts are the
# review ledger and the summary, which are committed.
corpus/results/
