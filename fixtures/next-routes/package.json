{
  "name": "next-routes",
  "version": "0.0.0",
  "private": true,
  "description": "A real Next.js App Router tree, so the adapter's path derivation can be checked against Next itself.",
  "scripts": {
    "dev": "next dev",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "next": "^15.5.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0"
  }
}
