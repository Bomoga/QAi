/**
 * Nothing configured on purpose.
 *
 * The point of this fixture is that Next routes it the way Next routes anything, so a
 * rewrite or a basePath here would make the conformance test agree with a configuration
 * rather than with the framework.
 */
export default {};
